doctype html
html(lang="en")
  head
    title #{Title}
    script(src='//ajax.googleapis.com/ajax/libs/angularjs/1.2.14/angular.min.js')
    script(src='//jashkenas.github.io/underscore/underscore-min.js')
    script(src='/static/main.js')
    link(rel='stylesheet', href='/static/main.css')
  body(ng-app='main' ng-controller='ctr')
    div.title.
      {{title}}
    .frame(ng-style='frame')
      .image(ng-style='style')
      #prev(ng-click='prev()')
        div(arrow-prev)
      #next(ng-click='next()')
        div(arrow-next)
    .bottom_panel(thumbs-gallery, ng-show='apps.settings.gallery')
    div.left_side
      button(ng-click='apps.settings.gallery=!apps.settings.gallery;') Gallery {{apps.settings.gallery ? 'on': 'off'}}
      div(ng-click='delete_img()') Delete
      div(ng-style='{border:"2px solid #aaa"}')
        div(ng-style='{border:"1px solid #bbb"}')
          b
            u Filters
        div(file-types)
